version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    # Bug detection
    - bodyclose
    - durationcheck
    - errcheck
    - govet
    - staticcheck

    # Code quality
    - ineffassign
    - unused
    - unconvert
    - unparam

    # Style
    - misspell

    # Security
    - gosec

  settings:
    gosec:
      excludes:
        # G704: SSRF taint analysis - this is a general-purpose HTTP client
        # that intentionally sends requests to caller-provided URLs.
        - G704

  exclusions:
    presets:
      - std-error-handling
      - common-false-positives
    rules:
      # Ignore errcheck/bodyclose/gosec in test files
      - path: _test\.go
        linters:
          - errcheck
          - bodyclose
          - gosec
      # Ignore gosec in example files (demonstration code)
      - path: example/
        linters:
          - gosec

formatters:
  enable:
    - gofmt
    - goimports
